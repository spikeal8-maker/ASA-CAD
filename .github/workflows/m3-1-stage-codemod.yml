name: M3.1 stage codemod

on:
  push:
    branches: [m3/m3-1-solve-overlay]
    paths:
      - scripts/m3/apply-m3-1-stage-wiring.mjs
      - .github/workflows/m3-1-stage-codemod.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: m3/m3-1-solve-overlay
      - name: Apply exact M3.1 stage wiring
        run: node scripts/m3/apply-m3-1-stage-wiring.mjs
      - name: Remove one-shot codemod files
        run: |
          rm scripts/m3/apply-m3-1-stage-wiring.mjs
          rm .github/workflows/m3-1-stage-codemod.yml
      - name: Commit M3.1 stage wiring
        run: |
          git config user.name "asa-cad-m3-bot"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git commit -m "M3.1: wire active Sketch solve overlay stage"
          git push origin HEAD:m3/m3-1-solve-overlay
