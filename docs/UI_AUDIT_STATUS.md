# ASA-CAD UI audit status

## Strong/defined

- six document kinds;
- command naming/grouping first wave;
- command lifecycle/ParameterPanel concept;
- workspace selection/navigation behavior;
- shortcuts architecture;
- phone/tablet interaction model;
- local client computation;
- standalone Docker and ASA Lab integration model.

## Newly defined by critical audit

- effective-resolution/DPI/browser-zoom model;
- HD/FHD/2K/4K/ultrawide regression matrix;
- width + height layout classes;
- desktop panel min/default/max geometry;
- top-chrome height budget;
- typography/readability floors;
- user UI Scale behavior;
- deterministic command-group collapse/overflow rules;
- high-DPI vector icon rule;
- canonical KOMPAS visual reference manifest/process;
- M2 visual acceptance gates;
- registry-v2 layout/mobile metadata requirements.

## Still open before claiming complete UI parity

- complete KOMPAS v25 functional inventory (#16);
- populate actual visual reference records with official/owner sources;
- measure owner-approved desktop references;
- implement command registry v2;
- real visual/browser/device test evidence;
- advanced KOMPAS workspaces classification.

Conclusion: the specification is now structured enough to proceed with M1 while M1U reference inventory continues, but M2 must not be declared visually complete until `M2_VISUAL_ACCEPTANCE.md` passes.